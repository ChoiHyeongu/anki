export interface RevealButtonProps {
  /** Callback when button is pressed (optional - card area handles tap) */
  onPress?: () => void;
  /** Whether the button is disabled */
  disabled?: boolean;
}
